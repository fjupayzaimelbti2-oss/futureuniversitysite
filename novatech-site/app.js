(function{ // set active link const page = document.documentElement.getAttribute('data-page') || ''; document.querySelectorAll('[data-nav]').forEach(a=>{ if(a.getAttribute('data-nav') === page){ a.setAttribute('aria-current', 'page'); } }); // mobile menu toggle const burger = document.getElementById('burger'); const mobile = document.getElementById('mobilemenu'); if(burger && mobile){ burger.addEventListener('click', =>{ const open = mobile.style.display === 'block'; mobile.style.display = open ? 'none' : 'block'; burger.setAttribute('aria-expanded', String(!open)); }); } // smooth anchor scroll on same page document.querySelectorAll('a[href^="#"]').forEach(a=>{ a.addEventListener('click', (e)=>{ const id = a.getAttribute('href'); const el = document.querySelector(id); if(el){ e.preventDefault; el.scrollIntoView({behavior:'smooth', block:'start'}); if(mobile) mobile.style.display = 'none'; } }); }); // admissions fake send window.fakeSend = function{ alert("Спасибо! Сообщение отправлено ."); };
});